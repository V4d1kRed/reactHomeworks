import React from 'react';
import {Typography} from "@mui/material";

const Title = () => {
  return (
    <Typography sx={{fontSize: '38px', textAlign: 'center', marginBottom: '10px'}} component="h1" variant="h5">
      Registration form
    </Typography>
  );
};

export default Title;
